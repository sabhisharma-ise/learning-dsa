// Collection of similar items 

#include <iostream>
using namespace std;

int main () {

    int A[5];   // Array Declaration

    int B[5] = {1, 2, 3, 4, 5}; // Array Initialization + Declaration

    // Printing array elements using loop
    for (int i = 0; i < 5; i++) {
        printf("%d ", B[i]);
    }

    return 0;
}